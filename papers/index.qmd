---
title: "📖 Papers"
subtitle: "探索前沿，建立知识图谱"
page-layout: full
listing:
  id: papers-listing
  contents: "../posts/papers/**/index.qmd"
  sort: "date desc"
  categories: true
  filter-ui: true
  page-size: 12
  type: default
---

::: {.paper-network-container}

::: {.network-title}
Knowledge Graph
:::

::: {#paper-network-3d .paper-network-3d}
:::

::: {.network-hint}
🖱️ 移动鼠标旋转视角 | 悬停气泡查看论文 | 点击进入详情
:::

:::

::: {.papers-stats}

::: {.stat-card}
[📚]{.stat-icon}
[<span id="paper-count">-</span>]{.stat-value}
[论文笔记]{.stat-desc}
:::

::: {.stat-card}
[🏷️]{.stat-icon}
[<span id="category-count">-</span>]{.stat-value}
[研究领域]{.stat-desc}
:::

::: {.stat-card}
[📅]{.stat-icon}
[<span id="latest-date">-</span>]{.stat-value}
[最近更新]{.stat-desc}
:::

:::

---

## 📑 All Papers

使用 **分类** 筛选研究领域，或直接搜索关键词。

::: {#papers-listing}
:::

```{=html}
<script>
(function() {
  // 等待 DOM 完全加载
  function init() {
    // ========== 1. 先创建 3D 场景（不依赖 listing） ==========
    var container = document.getElementById('paper-network-3d');
    if (!container) {
      console.error('paper-network-3d container not found');
      return;
    }
    
    // 创建 3D 场景
    var scene = document.createElement('div');
    scene.className = 'bubble-scene';
    container.appendChild(scene);
    
    // 中心节点（始终显示）
    var centerBubble = document.createElement('div');
    centerBubble.className = 'center-bubble';
    centerBubble.innerHTML = '🧠';
    scene.appendChild(centerBubble);
    
    // ========== 2. 收集论文数据 ==========
    var papers = [];
    
    // 方法1: 查找 listing 中的文章
    var listingEl = document.getElementById('papers-listing');
    if (listingEl) {
      // 查找所有文章链接
      var links = listingEl.querySelectorAll('a');
      links.forEach(function(link) {
        var href = link.getAttribute('href');
        var text = link.textContent.trim();
        // 过滤出文章链接（排除分类链接等）
        if (href && text && href.indexOf('posts/papers') !== -1 && text.length > 2) {
          // 避免重复
          var exists = papers.some(function(p) { return p.url === link.href; });
          if (!exists) {
            papers.push({
              title: text,
              url: link.href
            });
          }
        }
      });
    }
    
    // 方法2: 查找页面中所有指向论文的链接
    if (papers.length === 0) {
      var allLinks = document.querySelectorAll('a[href*="posts/papers"]');
      allLinks.forEach(function(link) {
        var text = link.textContent.trim();
        if (text && text.length > 3 && !link.closest('.paper-network-3d')) {
          var exists = papers.some(function(p) { return p.url === link.href; });
          if (!exists) {
            papers.push({
              title: text,
              url: link.href
            });
          }
        }
      });
    }
    
    console.log('Found papers:', papers);
    
    // ========== 3. 更新统计数字 ==========
    var paperCountEl = document.getElementById('paper-count');
    if (paperCountEl) {
      paperCountEl.textContent = papers.length > 0 ? papers.length : '0';
    }
    
    // 统计分类（简化：直接设置为论文数量）
    var categoryCountEl = document.getElementById('category-count');
    if (categoryCountEl) {
      categoryCountEl.textContent = papers.length > 0 ? '1' : '0';
    }
    
    // ========== 4. 生成气泡 ==========
    // 如果没有论文，显示示例气泡
    if (papers.length === 0) {
      papers.push({ title: '暂无论文', url: '#' });
    }
    
    var radius = 100;
    var total = papers.length;
    
    papers.forEach(function(paper, i) {
      var bubble = document.createElement('a');
      bubble.className = 'paper-bubble';
      bubble.href = paper.url;
      bubble.title = paper.title;
      
      // 球面分布算法
      var phi = Math.acos(-1 + (2 * i + 1) / total);
      var theta = Math.sqrt(total * Math.PI) * phi;
      
      var x = radius * Math.cos(theta) * Math.sin(phi);
      var y = radius * Math.sin(theta) * Math.sin(phi);
      var z = radius * Math.cos(phi);
      
      bubble.style.cssText = '--x:' + x + 'px; --y:' + y + 'px; --z:' + z + 'px;';
      
      var shortTitle = paper.title.length > 12 ? paper.title.substring(0, 12) + '...' : paper.title;
      bubble.innerHTML = '<span class="bubble-dot"></span><span class="bubble-label">' + shortTitle + '</span>';
      
      scene.appendChild(bubble);
    });
    
    // ========== 5. 鼠标跟随旋转 ==========
    var rotateX = 0, rotateY = 0;
    var targetRotateX = 0, targetRotateY = 0;
    
    container.onmousemove = function(e) {
      var rect = container.getBoundingClientRect();
      var centerX = rect.left + rect.width / 2;
      var centerY = rect.top + rect.height / 2;
      targetRotateY = ((e.clientX - centerX) / rect.width) * 25;
      targetRotateX = -((e.clientY - centerY) / rect.height) * 25;
    };
    
    container.onmouseleave = function() {
      targetRotateX = 0;
      targetRotateY = 0;
    };
    
    function animate() {
      rotateX += (targetRotateX - rotateX) * 0.08;
      rotateY += (targetRotateY - rotateY) * 0.08;
      scene.style.transform = 'rotateX(' + rotateX + 'deg) rotateY(' + rotateY + 'deg)';
      requestAnimationFrame(animate);
    }
    animate();
  }
  
  // 延迟执行，确保页面完全加载
  if (document.readyState === 'complete') {
    setTimeout(init, 300);
  } else {
    window.addEventListener('load', function() {
      setTimeout(init, 300);
    });
  }
})();
</script>
```
