---
title: "🛠️ Projects"
subtitle: "从论文到产品，工程实践全记录"
page-layout: full
listing:
  id: projects-listing
  contents: "../posts/projects/**/index.qmd"
  sort: "date desc"
  categories: true
  filter-ui: true
  page-size: 12
  type: default
---

::: {.projects-stats}

::: {.stat-card}
[🔧]{.stat-icon}
[<span id="project-count">-</span>]{.stat-value}
[项目数量]{.stat-desc}
:::

::: {.stat-card}
[🏷️]{.stat-icon}
[<span id="project-category-count">-</span>]{.stat-value}
[技术领域]{.stat-desc}
:::

::: {.stat-card}
[📅]{.stat-icon}
[<span id="project-latest-date">-</span>]{.stat-value}
[最近更新]{.stat-desc}
:::

:::

---

## 📂 All Projects

按时间排序，点击查看详细实现过程。每个项目页面包含完整的**开发流程**和**技术栈**说明。

::: {#projects-listing}
:::

```{=html}
<script>
(function() {
  function init() {
    var projects = [];
    
    // 查找 listing 中的项目
    var listingEl = document.getElementById('projects-listing');
    if (listingEl) {
      var links = listingEl.querySelectorAll('a');
      links.forEach(function(link) {
        var href = link.getAttribute('href');
        var text = link.textContent.trim();
        if (href && text && href.indexOf('posts/projects') !== -1 && text.length > 2) {
          var exists = projects.some(function(p) { return p.url === link.href; });
          if (!exists) {
            projects.push({ title: text, url: link.href });
          }
        }
      });
    }
    
    // 备用方案
    if (projects.length === 0) {
      var allLinks = document.querySelectorAll('a[href*="posts/projects"]');
      allLinks.forEach(function(link) {
        var text = link.textContent.trim();
        if (text && text.length > 3) {
          var exists = projects.some(function(p) { return p.url === link.href; });
          if (!exists) {
            projects.push({ title: text, url: link.href });
          }
        }
      });
    }
    
    console.log('Found projects:', projects);
    
    // 更新统计
    var projectCountEl = document.getElementById('project-count');
    if (projectCountEl) {
      projectCountEl.textContent = projects.length > 0 ? projects.length : '0';
    }
    
    var categoryCountEl = document.getElementById('project-category-count');
    if (categoryCountEl) {
      categoryCountEl.textContent = projects.length > 0 ? '1' : '0';
    }
  }
  
  if (document.readyState === 'complete') {
    setTimeout(init, 300);
  } else {
    window.addEventListener('load', function() {
      setTimeout(init, 300);
    });
  }
})();
</script>
```
